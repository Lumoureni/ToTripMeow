# 复制为 .env 后填入
PORT=3001
# 监听地址（0.0.0.0 允许局域网/公网访问）
HOST=0.0.0.0

# 生产环境 CORS 白名单（逗号分隔；留空则开发态允许全部来源）
# CORS_ORIGINS=https://trip.example.com,http://192.168.1.10:3001

# 高德 Web 服务 Key（仅服务端使用）
AMAP_KEY=

# 高德 JS API Key（前端地图显示，构建时注入）
VITE_AMAP_KEY=
VITE_AMAP_SECURITY_CODE=

# 管理员账号（首次启动自动创建；默认登录账号 admin / admin123，显示名「管理员」）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_DISPLAY_NAME=管理员

# --- 部署说明 ---
# 开发（局域网多人试用）：
#   npm run dev
#   同网段访问 http://<本机局域网IP>:5173
#
# 生产（单端口 API + 前端 / 云服务器）：
#   npm run build
#   npm start
#   详见 DEPLOY.md
#
# Render / Railway 公网部署：
#   详见 DEPLOY-PAAS.md（若分支含该文档）
